# Same as ../docker-compose.hostinger-manager.yml (project root).
# Run from /docker/ServicePortal:
#   docker compose -f docker-compose.hostinger-manager.yml up -d --build
#
# Do NOT run with -f deploy/... — use the root file above.
