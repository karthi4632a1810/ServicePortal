upstream paperzero_frontend {
    server frontend:80;
}

# HTTP — redirect to HTTPS when SSL certs are mounted
server {
    listen 80;
    server_name ${DOMAIN};

    client_max_body_size 12m;

    # Uncomment after placing certs in deploy/ssl/ (fullchain.pem + privkey.pem)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://paperzero_frontend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# HTTPS — enable when deploy/ssl/fullchain.pem and deploy/ssl/privkey.pem exist
# server {
#     listen 443 ssl http2;
#     server_name paper.mapims.edu.in;
#
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#     client_max_body_size 12m;
#
#     location / {
#         proxy_pass http://paperzero_frontend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#     }
# }
